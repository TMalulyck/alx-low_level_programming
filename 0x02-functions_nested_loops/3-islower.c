#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c: load a character to check
 * Return: 1 if lowercase, 0 otherwise if Capital
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
